# =============================================================================
# PostBot Environment Configuration Template
# =============================================================================
# IMPORTANT: Never commit .env.local or .env.production files to Git!
# They contain sensitive credentials and are already in .gitignore

# =============================================================================
# AUTHENTICATION PROVIDER CONFIGURATION
# =============================================================================
# Choose your auth provider: supabase, auth0, or clerk
# Default: supabase
AUTH_PROVIDER=supabase

# -----------------------------------------------------------------------------
# Option 1: Supabase Authentication (Recommended for getting started)
# -----------------------------------------------------------------------------
# Frontend (Vite) - Supabase
VITE_AUTH_PROVIDER=supabase
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Backend - Supabase
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_KEY=your-service-role-key-here

# -----------------------------------------------------------------------------
# Option 2: Auth0 Authentication
# -----------------------------------------------------------------------------
# Uncomment and configure if using Auth0:
# AUTH_PROVIDER=auth0
# 
# Frontend (Vite) - Auth0
# VITE_AUTH_PROVIDER=auth0
# VITE_AUTH0_DOMAIN=your-tenant.auth0.com
# VITE_AUTH0_CLIENT_ID=your-auth0-client-id
#
# Backend - Auth0
# AUTH0_DOMAIN=your-tenant.auth0.com
# AUTH0_CLIENT_ID=your-auth0-client-id
# AUTH0_CLIENT_SECRET=your-auth0-client-secret
# AUTH0_AUDIENCE=https://your-tenant.auth0.com/api/v2/

# -----------------------------------------------------------------------------
# Option 3: Clerk Authentication
# -----------------------------------------------------------------------------
# Uncomment and configure if using Clerk:
# AUTH_PROVIDER=clerk
#
# Frontend (Vite) - Clerk
# VITE_AUTH_PROVIDER=clerk
# VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
#
# Backend - Clerk
# CLERK_SECRET_KEY=sk_test_xxx
# CLERK_PUBLISHABLE_KEY=pk_test_xxx

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Your PostgreSQL database - can be ANY provider (local, AWS RDS, Cloud SQL, etc.)
# Supabase is used ONLY for authentication, NOT for your application data
DATABASE_URL=postgresql://user:password@localhost:5432/postbot_dev

# =============================================================================
# APPLICATION URLS
# =============================================================================
# Frontend URL (where auth callbacks redirect)
VITE_REDIRECT_URL=http://localhost:3000/app

# Backend API URL
VITE_API_URL=http://localhost:8000/api

# =============================================================================
# ENVIRONMENT
# =============================================================================
ENVIRONMENT=development

# =============================================================================
# INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env.local for local development
# 2. Copy this file to .env.production for production deployment
# 3. Fill in your actual credentials (never commit these files!)
# 4. Choose your auth provider and configure accordingly
#
# For Supabase setup:
#   - Create project at https://supabase.com
#   - Get URL and keys from Settings → API
#   - Enable OAuth providers in Authentication → Providers
#
# For Auth0 setup:
#   - Create application at https://auth0.com
#   - Get domain and credentials from Applications settings
#   - Configure callback URLs
#
# For Clerk setup:
#   - Create application at https://clerk.com
#   - Get keys from API Keys section
#   - Configure redirect URLs
