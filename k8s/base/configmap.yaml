apiVersion: v1
kind: ConfigMap
metadata:
  name: postbot-config
  namespace: postbot
data:
  # Non-sensitive configuration
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
  FRONTEND_PORT: "3000"
  BACKEND_PORT: "8000"
