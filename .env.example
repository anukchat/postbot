# ============================================================================
# Postbot (minimal .env)
# - Copy this file to .env and fill values.
# - Keep secrets out of git.
# ============================================================================

# --- Required (backend) -----------------------------------------------------
DATABASE_URL=postgresql://postgres:password@host:5432/postbot

# Auth provider (default is supabase)
AUTH_PROVIDER=supabase

# Backend auth (required when AUTH_PROVIDER=supabase)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key

# URLs used for CORS + redirects (defaults work for local Docker)
API_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# --- Required (frontend build) ---------------------------------------------
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
VITE_API_URL=http://localhost:8000
VITE_REDIRECT_URL=http://localhost:3000/app
VITE_ENV=development

# --- LLM (set at least ONE for generation) ---------------------------------
# GROQ_API_KEY=
# GEMINI_API_KEY=
# OPENROUTER_API_KEY=
# DEEPSEEK_API_KEY=
# OLLAMA_API_KEY=

# --- Optional integrations --------------------------------------------------
# REDDIT_CLIENT_ID=
# REDDIT_CLIENT_SECRET=
# REDDIT_USER_AGENT=
# SERPER_API_KEY=
# PIXABAY_API_KEY=

# --- Legacy aliases (avoid if possible) ------------------------------------
# If you have older configs that used these names, Settings still supports them:
# AUTH_PROVIDER_URL=
# AUTH_PROVIDER_KEY=